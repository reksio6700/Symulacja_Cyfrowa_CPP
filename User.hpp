#include "Stats.hpp"

class User
{
	private:
		double powerToBS1;
		double powerToBS2;
		int velocity;
		double distance;
		int TimeToSwitch; //TTT
		short int currentBTS;
	public:
		User();
		User(int newVelocity);
		~User();

		//getters, setters
		//TTT handling

};